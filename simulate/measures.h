namespace Measures
{
	double gallagher(Parliament p, vector<int> votes);
	double gallagherSS(vector<double>ss, vector<int> votes);
	double stlague(Parliament p, vector<int> votes);
	double stlagueSS(vector<double>ss, vector<int> votes);
	double cosine(Parliament p, vector<int> votes);
	double cosineSS(vector<double>ss, vector<int> votes);
	int govtsize(vector<double>ss);
	
};